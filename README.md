#我对HTTP的两种猜想
##第一种猜想
HTTP只能传HTML，但HTML里面可以包含各种格式的文件，所以HTTP也可以通过HTML传各种格式的文件。当服务器收到一个类似于**"http://127.0.0.1/IMG_2553.JPG"**的请求时，服务器会生成一个包含该图片的HTML，传送给浏览器。之后浏览器会渲染这个HTML界面，展示给用户。

##第二种猜想
HTTP可以传各种格式的文件（其中包括HTML），而浏览器在使用HTTP接到一个非HTML文件时，回解析改文件，并把其内容放进一个由浏览器生成的HTML文件中，展示给用户。